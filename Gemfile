source "https://rubygems.org"

gem "jekyll", "~> 4.3"

# Optional but recommended for production. Uncomment and `bundle install`,
# then add them under `plugins:` in _config.yml.
# gem "jekyll-seo-tag"
# gem "jekyll-sitemap"
